name: project_red_test
version: '1.0.0'
config-version: 2

profile: project_red_test

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  project_red_test:
    staging:
      +materialized: view
    marts:
      +materialized: table
